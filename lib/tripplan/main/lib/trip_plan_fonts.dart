/// TripPlan custom fonts (Holiday Calling loaded via pubspec).
class TripPlanFonts {
  static const String branding = 'HolidayCalling';

  static Future<void> ensureLoaded() async {
    // Font is bundled via pubspec; no runtime load required.
  }
}
